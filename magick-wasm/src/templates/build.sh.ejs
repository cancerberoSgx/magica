#!/bin/bash

# This file is auto-generated from src/templates

source <%= scriptsFolder %>/base.sh

<%= dontClean ? '' : 'rm -rf $PREFIX' %>

<%= addLib('zlib') %>

<%= addLib('libjpeg') %>

<%= addLib('libpng') %>

<%= addLib('tiff') %>

<%= addLib('jpeg-turbo') %>

<%= addLib('build-openjpeg') %>

<%= addLib('freetype') %>

<%= addLib('fftw') %>

<%= addLib('webp') %>

cd $CURRENT_DIR
source <%= scriptsFolder %>/build-ImageMagick.sh
testExitCode "build-ImageMagick" $?

cd $CURRENT_DIR
source <%= scriptsFolder %>/link-magickWasm.sh
testExitCode "link-magickWasm" $?

cd $CURRENT_DIR
source <%= scriptsFolder %>/print-revisions.sh
testExitCode "print-revisions" $?
